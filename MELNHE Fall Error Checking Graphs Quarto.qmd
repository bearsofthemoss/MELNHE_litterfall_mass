---
title: "MELNHE Fall Error Checking Graphs"
format: revealjs
editor: visual
---

```{r}

#Install packages:
library(ggplot2)
library(tidyr)
library(gridExtra)
library(RColorBrewer)

# FALL GRAPHS

#Import data
lit.all2 <- read.csv("C:/Users/ssonoknowles/Downloads/MELNHE Litterfall EDI Data - Final Data Sheet for EDI.csv") #This is the csv of the "Master Data Sheet" in the EDI file


#Isolate fall data
lit.fall.sort2<-subset(lit.all2, Season == "Fall" & Sorted == "Y")

#Convert to long form
lgf2<- gather(lit.fall.sort2[,c(1:41)], "SP","mass",c(18:41))
lgf2$staplo<-paste(lgf2$Stand, lgf2$Plot)
lgf2$Year<-as.factor(lgf2$Year)
lgf2$mass<-as.numeric(lgf2$mass)

#Pick a color palette
n <- 27 # 19 is for 19 'species'
qual_col_pals = brewer.pal.info[brewer.pal.info$category == 'qual',]
col_vector = unlist(mapply(brewer.pal, qual_col_pals$maxcolors, rownames(qual_col_pals)))

litcol<-col_vector

#streamline the basket labels so that there are only five
#This line is for all stands except for HBM, HBO, and JBM!!!!
simp_baskets <- c("LF1"="A1","LF2"="A3","LF3"="B2","LF4"="C1","LF5"="C3","1"="A1","2"="A3","3"="B2","4"="C1","5"="C3","A1"="A1","A3"="A3","B2"="B2","C1"="C1","C3"="C3")

lgf2$Basket <- simp_baskets[as.character(lgf2$Basket)]

#This line is for only HBM and JBM!
simp_baskets_HBM_JBM <- c("LF1"="A1","LF2"="A2","LF3"="CENTER","LF4"="B1","LF5"="B2","1"="A1","2"="A2","3"="CENTER","4"="B1","5"="B2","A1"="A1","A2"="A2","CENTER"="CENTER","B1"="B1","B2"="B2")

#This line is for only HBO!
simp_baskets_HBO <- c("LF1"="A1","LF2"="A3","LF3"="B2","LF4"="C1","LF5"="Y3","1"="A1","2"="A3","3"="B2","4"="C1","5"="Y3","A1"="A1","A3"="A3","B2"="B2","C1"="C1","Y3"="Y3")

# First, make Year a factor in correct order
lgf2$Year <- factor(lgf2$Year, levels = sort(unique(as.numeric(as.character(lgf2$Year)))))

# Subset
df_sub <- lgf2[lgf2$staplo == "C1 1",]

# Create plot
#baskets may not correspond with other baskets in the same panel across red dotted line
ggplot(df_sub, aes(x = Year, y = mass, fill = SP)) + 
  geom_bar(stat = "identity", col = "black") + 
  theme_bw() +
  facet_wrap(~Basket, scales = "fixed", nrow = 2) +
  scale_fill_manual(values = litcol[1:length(unique(df_sub$SP))]) +
  theme(axis.text.x = element_text(angle = 90, vjust = .5)) +
  ggtitle("Stand C1 1 over the years") +
  # Add vertical separator lines BETWEEN certain years
  geom_vline(xintercept = c(2.5, 4.5), linetype = "dashed", color = "red", size = 1)
```

------------------------------------------------------------------------

```{r}

# Subset
df_sub <- lgf2[lgf2$staplo == "C1 2",]

# Create plot
#baskets may not correspond with other baskets in the same panel across red dotted line
ggplot(df_sub, aes(x = Year, y = mass, fill = SP)) + 
  geom_bar(stat = "identity", col = "black") + 
  theme_bw() +
  facet_wrap(~Basket, scales = "fixed", nrow = 2) +
  scale_fill_manual(values = litcol[1:length(unique(df_sub$SP))]) +
  theme(axis.text.x = element_text(angle = 90, vjust = .5)) +
  ggtitle("Stand C1 2 over the years") +
  # Add vertical separator lines BETWEEN certain years
  geom_vline(xintercept = c(2.5, 4.5), linetype = "dashed", color = "red", size = 1)
```

------------------------------------------------------------------------

```{r}
# Subset
df_sub <- lgf2[lgf2$staplo == "C1 3",]

# Create plot
#baskets may not correspond with other baskets in the same panel across red dotted line
ggplot(df_sub, aes(x = Year, y = mass, fill = SP)) + 
  geom_bar(stat = "identity", col = "black") + 
  theme_bw() +
  facet_wrap(~Basket, scales = "fixed", nrow = 2) +
  scale_fill_manual(values = litcol[1:length(unique(df_sub$SP))]) +
  theme(axis.text.x = element_text(angle = 90, vjust = .5)) +
  ggtitle("Stand C1 3 over the years") +
  # Add vertical separator lines BETWEEN certain years
  geom_vline(xintercept = c(2.5, 4.5), linetype = "dashed", color = "red", size = 1)
```

------------------------------------------------------------------------

```{r}
# Subset
df_sub <- lgf2[lgf2$staplo == "C1 4",]

# Create plot
#baskets may not correspond with other baskets in the same panel across red dotted line
ggplot(df_sub, aes(x = Year, y = mass, fill = SP)) + 
  geom_bar(stat = "identity", col = "black") + 
  theme_bw() +
  facet_wrap(~Basket, scales = "fixed", nrow = 2) +
  scale_fill_manual(values = litcol[1:length(unique(df_sub$SP))]) +
  theme(axis.text.x = element_text(angle = 90, vjust = .5)) +
  ggtitle("Stand C1 4 over the years") +
  # Add vertical separator lines BETWEEN certain years
  geom_vline(xintercept = c(2.5), linetype = "dashed", color = "red", size = 1)
```

------------------------------------------------------------------------

```{r}

# Subset
df_sub <- lgf2[lgf2$staplo == "C2 1",]

# Create plot
#baskets may not correspond with other baskets in the same panel across red dotted line
ggplot(df_sub, aes(x = Year, y = mass, fill = SP)) + 
  geom_bar(stat = "identity", col = "black") + 
  theme_bw() +
  facet_wrap(~Basket, scales = "fixed", nrow = 2) +
  scale_fill_manual(values = litcol[1:length(unique(df_sub$SP))]) +
  theme(axis.text.x = element_text(angle = 90, vjust = .5)) +
  ggtitle("Stand C2 1 over the years") +
  # Add vertical separator lines BETWEEN certain years
  geom_vline(xintercept = c(2.5, 4.5), linetype = "dashed", color = "red", size = 1)
```

------------------------------------------------------------------------

```{r}
# Subset
df_sub <- lgf2[lgf2$staplo == "C2 2",]

# Create plot
#baskets may not correspond with other baskets in the same panel across red dotted line
ggplot(df_sub, aes(x = Year, y = mass, fill = SP)) + 
  geom_bar(stat = "identity", col = "black") + 
  theme_bw() +
  facet_wrap(~Basket, scales = "fixed", nrow = 2) +
  scale_fill_manual(values = litcol[1:length(unique(df_sub$SP))]) +
  theme(axis.text.x = element_text(angle = 90, vjust = .5)) +
  ggtitle("Stand C2 2 over the years") +
  # Add vertical separator lines BETWEEN certain years
  geom_vline(xintercept = c(2.5, 4.5), linetype = "dashed", color = "red", size = 1)
```

------------------------------------------------------------------------

```{r}
# Subset
df_sub <- lgf2[lgf2$staplo == "C2 3",]

# Create plot
#baskets may not correspond with other baskets in the same panel across red dotted line
ggplot(df_sub, aes(x = Year, y = mass, fill = SP)) + 
  geom_bar(stat = "identity", col = "black") + 
  theme_bw() +
  facet_wrap(~Basket, scales = "fixed", nrow = 2) +
  scale_fill_manual(values = litcol[1:length(unique(df_sub$SP))]) +
  theme(axis.text.x = element_text(angle = 90, vjust = .5)) +
  ggtitle("Stand C2 3 over the years") +
  # Add vertical separator lines BETWEEN certain years
  geom_vline(xintercept = c(2.5, 4.5), linetype = "dashed", color = "red", size = 1)
```

------------------------------------------------------------------------

```{r}
# Subset
df_sub <- lgf2[lgf2$staplo == "C2 4",]

# Create plot
#baskets may not correspond with other baskets in the same panel across red dotted line
ggplot(df_sub, aes(x = Year, y = mass, fill = SP)) + 
  geom_bar(stat = "identity", col = "black") + 
  theme_bw() +
  facet_wrap(~Basket, scales = "fixed", nrow = 2) +
  scale_fill_manual(values = litcol[1:length(unique(df_sub$SP))]) +
  theme(axis.text.x = element_text(angle = 90, vjust = .5)) +
  ggtitle("Stand C2 4 over the years") +
  # Add vertical separator lines BETWEEN certain years
  geom_vline(xintercept = c(2.5), linetype = "dashed", color = "red", size = 1)
```

------------------------------------------------------------------------

```{r}

# Subset
df_sub <- lgf2[lgf2$staplo == "C3 1",]

# Create plot
#baskets may not correspond with other baskets in the same panel across red dotted line
ggplot(df_sub, aes(x = Year, y = mass, fill = SP)) + 
  geom_bar(stat = "identity", col = "black") + 
  theme_bw() +
  facet_wrap(~Basket, scales = "fixed", nrow = 2) +
  scale_fill_manual(values = litcol[1:length(unique(df_sub$SP))]) +
  theme(axis.text.x = element_text(angle = 90, vjust = .5)) +
  ggtitle("Stand C3 1 over the years") +
  # Add vertical separator lines BETWEEN certain years
  geom_vline(xintercept = c(2.5), linetype = "dashed", color = "red", size = 1)
```

------------------------------------------------------------------------

```{r}
# Subset
df_sub <- lgf2[lgf2$staplo == "C3 2",]

# Create plot
#baskets may not correspond with other baskets in the same panel across red dotted line
ggplot(df_sub, aes(x = Year, y = mass, fill = SP)) + 
  geom_bar(stat = "identity", col = "black") + 
  theme_bw() +
  facet_wrap(~Basket, scales = "fixed", nrow = 2) +
  scale_fill_manual(values = litcol[1:length(unique(df_sub$SP))]) +
  theme(axis.text.x = element_text(angle = 90, vjust = .5)) +
  ggtitle("Stand C3 2 over the years") +
  # Add vertical separator lines BETWEEN certain years
  geom_vline(xintercept = c(2.5), linetype = "dashed", color = "red", size = 1)
```

------------------------------------------------------------------------

```{r}
# Subset
df_sub <- lgf2[lgf2$staplo == "C3 3",]

# Create plot
#baskets may not correspond with other baskets in the same panel across red dotted line
ggplot(df_sub, aes(x = Year, y = mass, fill = SP)) + 
  geom_bar(stat = "identity", col = "black") + 
  theme_bw() +
  facet_wrap(~Basket, scales = "fixed", nrow = 2) +
  scale_fill_manual(values = litcol[1:length(unique(df_sub$SP))]) +
  theme(axis.text.x = element_text(angle = 90, vjust = .5)) +
  ggtitle("Stand C3 3 over the years") +
  # Add vertical separator lines BETWEEN certain years
  geom_vline(xintercept = c(2.5), linetype = "dashed", color = "red", size = 1)
```

------------------------------------------------------------------------

```{r}
# Subset
df_sub <- lgf2[lgf2$staplo == "C3 4",]

# Create plot
#baskets may not correspond with other baskets in the same panel across red dotted line
ggplot(df_sub, aes(x = Year, y = mass, fill = SP)) + 
  geom_bar(stat = "identity", col = "black") + 
  theme_bw() +
  facet_wrap(~Basket, scales = "fixed", nrow = 2) +
  scale_fill_manual(values = litcol[1:length(unique(df_sub$SP))]) +
  theme(axis.text.x = element_text(angle = 90, vjust = .5)) +
  ggtitle("Stand C3 4 over the years")
```

------------------------------------------------------------------------

```{r}

# Subset
df_sub <- lgf2[lgf2$staplo == "C4 1",]

# Create plot
#baskets may not correspond with other baskets in the same panel across red dotted line
ggplot(df_sub, aes(x = Year, y = mass, fill = SP)) + 
  geom_bar(stat = "identity", col = "black") + 
  theme_bw() +
  facet_wrap(~Basket, scales = "fixed", nrow = 2) +
  scale_fill_manual(values = litcol[1:length(unique(df_sub$SP))]) +
  theme(axis.text.x = element_text(angle = 90, vjust = .5)) +
  ggtitle("Stand C4 1 over the years") +
  # Add vertical separator lines BETWEEN certain years
  geom_vline(xintercept = c(2.5, 4.5), linetype = "dashed", color = "red", size = 1)
```

------------------------------------------------------------------------

```{r}
# Subset
df_sub <- lgf2[lgf2$staplo == "C4 2",]

# Create plot
#baskets may not correspond with other baskets in the same panel across red dotted line
ggplot(df_sub, aes(x = Year, y = mass, fill = SP)) + 
  geom_bar(stat = "identity", col = "black") + 
  theme_bw() +
  facet_wrap(~Basket, scales = "fixed", nrow = 2) +
  scale_fill_manual(values = litcol[1:length(unique(df_sub$SP))]) +
  theme(axis.text.x = element_text(angle = 90, vjust = .5)) +
  ggtitle("Stand C4 2 over the years") +
  # Add vertical separator lines BETWEEN certain years
  geom_vline(xintercept = c(2.5, 4.5), linetype = "dashed", color = "red", size = 1)
```

------------------------------------------------------------------------

```{r}
# Subset
df_sub <- lgf2[lgf2$staplo == "C4 3",]

# Create plot
#baskets may not correspond with other baskets in the same panel across red dotted line
ggplot(df_sub, aes(x = Year, y = mass, fill = SP)) + 
  geom_bar(stat = "identity", col = "black") + 
  theme_bw() +
  facet_wrap(~Basket, scales = "fixed", nrow = 2) +
  scale_fill_manual(values = litcol[1:length(unique(df_sub$SP))]) +
  theme(axis.text.x = element_text(angle = 90, vjust = .5)) +
  ggtitle("Stand C4 3 over the years") +
  # Add vertical separator lines BETWEEN certain years
  geom_vline(xintercept = c(2.5, 4.5), linetype = "dashed", color = "red", size = 1)
```

------------------------------------------------------------------------

```{r}
# Subset
df_sub <- lgf2[lgf2$staplo == "C4 4",]

# Create plot
#baskets may not correspond with other baskets in the same panel across red dotted line
ggplot(df_sub, aes(x = Year, y = mass, fill = SP)) + 
  geom_bar(stat = "identity", col = "black") + 
  theme_bw() +
  facet_wrap(~Basket, scales = "fixed", nrow = 2) +
  scale_fill_manual(values = litcol[1:length(unique(df_sub$SP))]) +
  theme(axis.text.x = element_text(angle = 90, vjust = .5)) +
  ggtitle("Stand C4 4 over the years") +
  # Add vertical separator lines BETWEEN certain years
  geom_vline(xintercept = c(2.5), linetype = "dashed", color = "red", size = 1)
```

------------------------------------------------------------------------

```{r}

# Subset
df_sub <- lgf2[lgf2$staplo == "C5 1",]

# Create plot
#baskets may not correspond with other baskets in the same panel across red dotted line
ggplot(df_sub, aes(x = Year, y = mass, fill = SP)) + 
  geom_bar(stat = "identity", col = "black") + 
  theme_bw() +
  facet_wrap(~Basket, scales = "fixed", nrow = 2) +
  scale_fill_manual(values = litcol[1:length(unique(df_sub$SP))]) +
  theme(axis.text.x = element_text(angle = 90, vjust = .5)) +
  ggtitle("Stand C5 1 over the years") +
  # Add vertical separator lines BETWEEN certain years
  geom_vline(xintercept = c(2.5), linetype = "dashed", color = "red", size = 1)
```

------------------------------------------------------------------------

```{r}
# Subset
df_sub <- lgf2[lgf2$staplo == "C5 2",]

# Create plot
#baskets may not correspond with other baskets in the same panel across red dotted line
ggplot(df_sub, aes(x = Year, y = mass, fill = SP)) + 
  geom_bar(stat = "identity", col = "black") + 
  theme_bw() +
  facet_wrap(~Basket, scales = "fixed", nrow = 2) +
  scale_fill_manual(values = litcol[1:length(unique(df_sub$SP))]) +
  theme(axis.text.x = element_text(angle = 90, vjust = .5)) +
  ggtitle("Stand C5 2 over the years") +
  # Add vertical separator lines BETWEEN certain years
  geom_vline(xintercept = c(2.5), linetype = "dashed", color = "red", size = 1)
```

------------------------------------------------------------------------

```{r}
# Subset
df_sub <- lgf2[lgf2$staplo == "C5 3",]

# Create plot
#baskets may not correspond with other baskets in the same panel across red dotted line
ggplot(df_sub, aes(x = Year, y = mass, fill = SP)) + 
  geom_bar(stat = "identity", col = "black") + 
  theme_bw() +
  facet_wrap(~Basket, scales = "fixed", nrow = 2) +
  scale_fill_manual(values = litcol[1:length(unique(df_sub$SP))]) +
  theme(axis.text.x = element_text(angle = 90, vjust = .5)) +
  ggtitle("Stand C5 3 over the years") +
  # Add vertical separator lines BETWEEN certain years
  geom_vline(xintercept = c(2.5), linetype = "dashed", color = "red", size = 1)
```

------------------------------------------------------------------------

```{r}
# Subset
df_sub <- lgf2[lgf2$staplo == "C5 4",]

# Create plot
#baskets may not correspond with other baskets in the same panel across red dotted line
ggplot(df_sub, aes(x = Year, y = mass, fill = SP)) + 
  geom_bar(stat = "identity", col = "black") + 
  theme_bw() +
  facet_wrap(~Basket, scales = "fixed", nrow = 2) +
  scale_fill_manual(values = litcol[1:length(unique(df_sub$SP))]) +
  theme(axis.text.x = element_text(angle = 90, vjust = .5)) +
  ggtitle("Stand C5 4 over the years")
```

------------------------------------------------------------------------

```{r}

# Subset
df_sub <- lgf2[lgf2$staplo == "C6 1",]

# Create plot
#baskets may not correspond with other baskets in the same panel across red dotted line
ggplot(df_sub, aes(x = Year, y = mass, fill = SP)) + 
  geom_bar(stat = "identity", col = "black") + 
  theme_bw() +
  facet_wrap(~Basket, scales = "fixed", nrow = 2) +
  scale_fill_manual(values = litcol[1:length(unique(df_sub$SP))]) +
  theme(axis.text.x = element_text(angle = 90, vjust = .5)) +
  ggtitle("Stand C6 1 over the years") +
  # Add vertical separator lines BETWEEN certain years
  geom_vline(xintercept = c(2.5, 4.5), linetype = "dashed", color = "red", size = 1)
```

------------------------------------------------------------------------

```{r}
# Subset
df_sub <- lgf2[lgf2$staplo == "C6 2",]

# Create plot
#baskets may not correspond with other baskets in the same panel across red dotted line
ggplot(df_sub, aes(x = Year, y = mass, fill = SP)) + 
  geom_bar(stat = "identity", col = "black") + 
  theme_bw() +
  facet_wrap(~Basket, scales = "fixed", nrow = 2) +
  scale_fill_manual(values = litcol[1:length(unique(df_sub$SP))]) +
  theme(axis.text.x = element_text(angle = 90, vjust = .5)) +
  ggtitle("Stand C6 2 over the years") +
  # Add vertical separator lines BETWEEN certain years
  geom_vline(xintercept = c(2.5, 4.5), linetype = "dashed", color = "red", size = 1)
```

------------------------------------------------------------------------

```{r}
# Subset
df_sub <- lgf2[lgf2$staplo == "C6 3",]

# Create plot
#baskets may not correspond with other baskets in the same panel across red dotted line
ggplot(df_sub, aes(x = Year, y = mass, fill = SP)) + 
  geom_bar(stat = "identity", col = "black") + 
  theme_bw() +
  facet_wrap(~Basket, scales = "fixed", nrow = 2) +
  scale_fill_manual(values = litcol[1:length(unique(df_sub$SP))]) +
  theme(axis.text.x = element_text(angle = 90, vjust = .5)) +
  ggtitle("Stand C6 3 over the years") +
  # Add vertical separator lines BETWEEN certain years
  geom_vline(xintercept = c(2.5, 4.5), linetype = "dashed", color = "red", size = 1)
```

------------------------------------------------------------------------

```{r}
# Subset
df_sub <- lgf2[lgf2$staplo == "C6 4",]

# Create plot
#baskets may not correspond with other baskets in the same panel across red dotted line
ggplot(df_sub, aes(x = Year, y = mass, fill = SP)) + 
  geom_bar(stat = "identity", col = "black") + 
  theme_bw() +
  facet_wrap(~Basket, scales = "fixed", nrow = 2) +
  scale_fill_manual(values = litcol[1:length(unique(df_sub$SP))]) +
  theme(axis.text.x = element_text(angle = 90, vjust = .5)) +
  ggtitle("Stand C6 4 over the years") +
  # Add vertical separator lines BETWEEN certain years
  geom_vline(xintercept = c(2.5), linetype = "dashed", color = "red", size = 1)
```

------------------------------------------------------------------------

```{r}

# Subset
df_sub <- lgf2[lgf2$staplo == "C7 1",]

# Create plot
#baskets may not correspond with other baskets in the same panel across red dotted line
ggplot(df_sub, aes(x = Year, y = mass, fill = SP)) + 
  geom_bar(stat = "identity", col = "black") + 
  theme_bw() +
  facet_wrap(~Basket, scales = "fixed", nrow = 2) +
  scale_fill_manual(values = litcol[1:length(unique(df_sub$SP))]) +
  theme(axis.text.x = element_text(angle = 90, vjust = .5)) +
  ggtitle("Stand C7 1 over the years") +
  # Add vertical separator lines BETWEEN certain years
  geom_vline(xintercept = c(2.5), linetype = "dashed", color = "red", size = 1)
```

------------------------------------------------------------------------

```{r}
# Subset
df_sub <- lgf2[lgf2$staplo == "C7 2",]

# Create plot
#baskets may not correspond with other baskets in the same panel across red dotted line
ggplot(df_sub, aes(x = Year, y = mass, fill = SP)) + 
  geom_bar(stat = "identity", col = "black") + 
  theme_bw() +
  facet_wrap(~Basket, scales = "fixed", nrow = 2) +
  scale_fill_manual(values = litcol[1:length(unique(df_sub$SP))]) +
  theme(axis.text.x = element_text(angle = 90, vjust = .5)) +
  ggtitle("Stand C7 2 over the years") +
  # Add vertical separator lines BETWEEN certain years
  geom_vline(xintercept = c(2.5), linetype = "dashed", color = "red", size = 1)
```

------------------------------------------------------------------------

```{r}
# Subset
df_sub <- lgf2[lgf2$staplo == "C7 3",]

# Create plot
#baskets may not correspond with other baskets in the same panel across red dotted line
ggplot(df_sub, aes(x = Year, y = mass, fill = SP)) + 
  geom_bar(stat = "identity", col = "black") + 
  theme_bw() +
  facet_wrap(~Basket, scales = "fixed", nrow = 2) +
  scale_fill_manual(values = litcol[1:length(unique(df_sub$SP))]) +
  theme(axis.text.x = element_text(angle = 90, vjust = .5)) +
  ggtitle("Stand C7 3 over the years") +
  # Add vertical separator lines BETWEEN certain years
  geom_vline(xintercept = c(2.5), linetype = "dashed", color = "red", size = 1)
```

------------------------------------------------------------------------

```{r}
# Subset
df_sub <- lgf2[lgf2$staplo == "C7 4",]

# Create plot
#baskets may not correspond with other baskets in the same panel across red dotted line
ggplot(df_sub, aes(x = Year, y = mass, fill = SP)) + 
  geom_bar(stat = "identity", col = "black") + 
  theme_bw() +
  facet_wrap(~Basket, scales = "fixed", nrow = 2) +
  scale_fill_manual(values = litcol[1:length(unique(df_sub$SP))]) +
  theme(axis.text.x = element_text(angle = 90, vjust = .5)) +
  ggtitle("Stand C7 4 over the years")
```

------------------------------------------------------------------------

```{r}

# Subset
df_sub <- lgf2[lgf2$staplo == "C8 1",]

# Create plot
#baskets may not correspond with other baskets in the same panel across red dotted line
ggplot(df_sub, aes(x = Year, y = mass, fill = SP)) + 
  geom_bar(stat = "identity", col = "black") + 
  theme_bw() +
  facet_wrap(~Basket, scales = "fixed", nrow = 2) +
  scale_fill_manual(values = litcol[1:length(unique(df_sub$SP))]) +
  theme(axis.text.x = element_text(angle = 90, vjust = .5)) +
  ggtitle("Stand C8 1 over the years") +
  # Add vertical separator lines BETWEEN certain years
  geom_vline(xintercept = c(2.5, 4.5), linetype = "dashed", color = "red", size = 1)
```

------------------------------------------------------------------------

```{r}
# Subset
df_sub <- lgf2[lgf2$staplo == "C8 2",]

# Create plot
#baskets may not correspond with other baskets in the same panel across red dotted line
ggplot(df_sub, aes(x = Year, y = mass, fill = SP)) + 
  geom_bar(stat = "identity", col = "black") + 
  theme_bw() +
  facet_wrap(~Basket, scales = "fixed", nrow = 2) +
  scale_fill_manual(values = litcol[1:length(unique(df_sub$SP))]) +
  theme(axis.text.x = element_text(angle = 90, vjust = .5)) +
  ggtitle("Stand C8 2 over the years") +
  # Add vertical separator lines BETWEEN certain years
  geom_vline(xintercept = c(2.5, 4.5), linetype = "dashed", color = "red", size = 1)
```

------------------------------------------------------------------------

```{r}
# Subset
df_sub <- lgf2[lgf2$staplo == "C8 3",]

# Create plot
#baskets may not correspond with other baskets in the same panel across red dotted line
ggplot(df_sub, aes(x = Year, y = mass, fill = SP)) + 
  geom_bar(stat = "identity", col = "black") + 
  theme_bw() +
  facet_wrap(~Basket, scales = "fixed", nrow = 2) +
  scale_fill_manual(values = litcol[1:length(unique(df_sub$SP))]) +
  theme(axis.text.x = element_text(angle = 90, vjust = .5)) +
  ggtitle("Stand C8 3 over the years") +
  # Add vertical separator lines BETWEEN certain years
  geom_vline(xintercept = c(2.5, 4.5), linetype = "dashed", color = "red", size = 1)
```

------------------------------------------------------------------------

```{r}
# Subset
df_sub <- lgf2[lgf2$staplo == "C8 4",]

# Create plot
#baskets may not correspond with other baskets in the same panel across red dotted line
ggplot(df_sub, aes(x = Year, y = mass, fill = SP)) + 
  geom_bar(stat = "identity", col = "black") + 
  theme_bw() +
  facet_wrap(~Basket, scales = "fixed", nrow = 2) +
  scale_fill_manual(values = litcol[1:length(unique(df_sub$SP))]) +
  theme(axis.text.x = element_text(angle = 90, vjust = .5)) +
  ggtitle("Stand C8 4 over the years") +
  # Add vertical separator lines BETWEEN certain years
  geom_vline(xintercept = c(2.5), linetype = "dashed", color = "red", size = 1)
```

------------------------------------------------------------------------

```{r}

# Subset
df_sub <- lgf2[lgf2$staplo == "C9 1",]

# Create plot
#baskets may not correspond with other baskets in the same panel across red dotted line
ggplot(df_sub, aes(x = Year, y = mass, fill = SP)) + 
  geom_bar(stat = "identity", col = "black") + 
  theme_bw() +
  facet_wrap(~Basket, scales = "fixed", nrow = 2) +
  scale_fill_manual(values = litcol[1:length(unique(df_sub$SP))]) +
  theme(axis.text.x = element_text(angle = 90, vjust = .5)) +
  ggtitle("Stand C9 1 over the years") +
  # Add vertical separator lines BETWEEN certain years
  geom_vline(xintercept = c(2.5, 4.5), linetype = "dashed", color = "red", size = 1)
```

------------------------------------------------------------------------

```{r}
# Subset
df_sub <- lgf2[lgf2$staplo == "C9 2",]

# Create plot
#baskets may not correspond with other baskets in the same panel across red dotted line
ggplot(df_sub, aes(x = Year, y = mass, fill = SP)) + 
  geom_bar(stat = "identity", col = "black") + 
  theme_bw() +
  facet_wrap(~Basket, scales = "fixed", nrow = 2) +
  scale_fill_manual(values = litcol[1:length(unique(df_sub$SP))]) +
  theme(axis.text.x = element_text(angle = 90, vjust = .5)) +
  ggtitle("Stand C9 2 over the years") +
  # Add vertical separator lines BETWEEN certain years
  geom_vline(xintercept = c(2.5, 4.5), linetype = "dashed", color = "red", size = 1)
```

------------------------------------------------------------------------

```{r}
# Subset
df_sub <- lgf2[lgf2$staplo == "C9 3",]

# Create plot
#baskets may not correspond with other baskets in the same panel across red dotted line
ggplot(df_sub, aes(x = Year, y = mass, fill = SP)) + 
  geom_bar(stat = "identity", col = "black") + 
  theme_bw() +
  facet_wrap(~Basket, scales = "fixed", nrow = 2) +
  scale_fill_manual(values = litcol[1:length(unique(df_sub$SP))]) +
  theme(axis.text.x = element_text(angle = 90, vjust = .5)) +
  ggtitle("Stand C9 3 over the years") +
  # Add vertical separator lines BETWEEN certain years
  geom_vline(xintercept = c(2.5, 4.5), linetype = "dashed", color = "red", size = 1)
```

------------------------------------------------------------------------

```{r}
# Subset
df_sub <- lgf2[lgf2$staplo == "C9 4",]

# Create plot
#baskets may not correspond with other baskets in the same panel across red dotted line
ggplot(df_sub, aes(x = Year, y = mass, fill = SP)) + 
  geom_bar(stat = "identity", col = "black") + 
  theme_bw() +
  facet_wrap(~Basket, scales = "fixed", nrow = 2) +
  scale_fill_manual(values = litcol[1:length(unique(df_sub$SP))]) +
  theme(axis.text.x = element_text(angle = 90, vjust = .5)) +
  ggtitle("Stand C9 4 over the years") +
  # Add vertical separator lines BETWEEN certain years
  geom_vline(xintercept = c(2.5), linetype = "dashed", color = "red", size = 1)
```

------------------------------------------------------------------------

```{r}
lgf2$Basket <- simp_baskets_HBM_JBM[as.character(lgf2$Basket)]

# Subset
df_sub <- lgf2[lgf2$staplo == "HBM 1",]

# Create plot
#baskets may not correspond with other baskets in the same panel across red dotted line
ggplot(df_sub, aes(x = Year, y = mass, fill = SP)) + 
  geom_bar(stat = "identity", col = "black") + 
  theme_bw() +
  facet_wrap(~Basket, scales = "fixed", nrow = 2) +
  scale_fill_manual(values = litcol[1:length(unique(df_sub$SP))]) +
  theme(axis.text.x = element_text(angle = 90, vjust = .5)) +
  ggtitle("Stand HBM 1 over the years") +
  # Add vertical separator lines BETWEEN certain years
  geom_vline(xintercept = c(3.5), linetype = "dashed", color = "red", size = 1)
```

------------------------------------------------------------------------

```{r}
# Subset
df_sub <- lgf2[lgf2$staplo == "HBM 2",]

# Create plot
#baskets may not correspond with other baskets in the same panel across red dotted line
ggplot(df_sub, aes(x = Year, y = mass, fill = SP)) + 
  geom_bar(stat = "identity", col = "black") + 
  theme_bw() +
  facet_wrap(~Basket, scales = "fixed", nrow = 2) +
  scale_fill_manual(values = litcol[1:length(unique(df_sub$SP))]) +
  theme(axis.text.x = element_text(angle = 90, vjust = .5)) +
  ggtitle("Stand HBM 2 over the years") +
  # Add vertical separator lines BETWEEN certain years
  geom_vline(xintercept = c(3.5), linetype = "dashed", color = "red", size = 1)
```

------------------------------------------------------------------------

```{r}
# Subset
df_sub <- lgf2[lgf2$staplo == "HBM 3",]

# Create plot
#baskets may not correspond with other baskets in the same panel across red dotted line
ggplot(df_sub, aes(x = Year, y = mass, fill = SP)) + 
  geom_bar(stat = "identity", col = "black") + 
  theme_bw() +
  facet_wrap(~Basket, scales = "fixed", nrow = 2) +
  scale_fill_manual(values = litcol[1:length(unique(df_sub$SP))]) +
  theme(axis.text.x = element_text(angle = 90, vjust = .5)) +
  ggtitle("Stand HBM 3 over the years") +
  # Add vertical separator lines BETWEEN certain years
  geom_vline(xintercept = c(3.5), linetype = "dashed", color = "red", size = 1)
```

------------------------------------------------------------------------

```{r}
# Subset
df_sub <- lgf2[lgf2$staplo == "HBM 4",]

# Create plot
#baskets may not correspond with other baskets in the same panel across red dotted line
ggplot(df_sub, aes(x = Year, y = mass, fill = SP)) + 
  geom_bar(stat = "identity", col = "black") + 
  theme_bw() +
  facet_wrap(~Basket, scales = "fixed", nrow = 2) +
  scale_fill_manual(values = litcol[1:length(unique(df_sub$SP))]) +
  theme(axis.text.x = element_text(angle = 90, vjust = .5)) +
  ggtitle("Stand HBM 4 over the years") +
  # Add vertical separator lines BETWEEN certain years
  geom_vline(xintercept = c(2.5), linetype = "dashed", color = "red", size = 1)
```

------------------------------------------------------------------------

```{r}

lgf2$Basket <- simp_baskets_HBO[as.character(lgf2$Basket)]

# Subset
df_sub <- lgf2[lgf2$staplo == "HBO 1",]

# Create plot
#baskets may not correspond with other baskets in the same panel across red dotted line
ggplot(df_sub, aes(x = Year, y = mass, fill = SP)) + 
  geom_bar(stat = "identity", col = "black") + 
  theme_bw() +
  facet_wrap(~Basket, scales = "fixed", nrow = 2) +
  scale_fill_manual(values = litcol[1:length(unique(df_sub$SP))]) +
  theme(axis.text.x = element_text(angle = 90, vjust = .5)) +
  ggtitle("Stand HBO 1 over the years") +
  # Add vertical separator lines BETWEEN certain years
  geom_vline(xintercept = c(3.5), linetype = "dashed", color = "red", size = 1)
```

------------------------------------------------------------------------

```{r}
# Subset
df_sub <- lgf2[lgf2$staplo == "HBO 2",]

# Create plot
#baskets may not correspond with other baskets in the same panel across red dotted line
ggplot(df_sub, aes(x = Year, y = mass, fill = SP)) + 
  geom_bar(stat = "identity", col = "black") + 
  theme_bw() +
  facet_wrap(~Basket, scales = "fixed", nrow = 2) +
  scale_fill_manual(values = litcol[1:length(unique(df_sub$SP))]) +
  theme(axis.text.x = element_text(angle = 90, vjust = .5)) +
  ggtitle("Stand HBO 2 over the years") +
  # Add vertical separator lines BETWEEN certain years
  geom_vline(xintercept = c(3.5), linetype = "dashed", color = "red", size = 1)
```

------------------------------------------------------------------------

```{r}
# Subset
df_sub <- lgf2[lgf2$staplo == "HBO 3",]

# Create plot
#baskets may not correspond with other baskets in the same panel across red dotted line
ggplot(df_sub, aes(x = Year, y = mass, fill = SP)) + 
  geom_bar(stat = "identity", col = "black") + 
  theme_bw() +
  facet_wrap(~Basket, scales = "fixed", nrow = 2) +
  scale_fill_manual(values = litcol[1:length(unique(df_sub$SP))]) +
  theme(axis.text.x = element_text(angle = 90, vjust = .5)) +
  ggtitle("Stand HBO 3 over the years") +
  # Add vertical separator lines BETWEEN certain years
  geom_vline(xintercept = c(3.5), linetype = "dashed", color = "red", size = 1)
```

------------------------------------------------------------------------

```{r}
# Subset
df_sub <- lgf2[lgf2$staplo == "HBO 4",]

# Create plot
#baskets may not correspond with other baskets in the same panel across red dotted line
ggplot(df_sub, aes(x = Year, y = mass, fill = SP)) + 
  geom_bar(stat = "identity", col = "black") + 
  theme_bw() +
  facet_wrap(~Basket, scales = "fixed", nrow = 2) +
  scale_fill_manual(values = litcol[1:length(unique(df_sub$SP))]) +
  theme(axis.text.x = element_text(angle = 90, vjust = .5)) +
  ggtitle("Stand HBO 4 over the years") +
  # Add vertical separator lines BETWEEN certain years
  geom_vline(xintercept = c(2.5), linetype = "dashed", color = "red", size = 1)
```

------------------------------------------------------------------------

```{r}

lgf2$Basket <- simp_baskets_HBM_JBM[as.character(lgf2$Basket)]

# Subset
df_sub <- lgf2[lgf2$staplo == "JBM 1",]

# Create plot
#baskets may not correspond with other baskets in the same panel across red dotted line
ggplot(df_sub, aes(x = Year, y = mass, fill = SP)) + 
  geom_bar(stat = "identity", col = "black") + 
  theme_bw() +
  facet_wrap(~Basket, scales = "fixed", nrow = 2) +
  scale_fill_manual(values = litcol[1:length(unique(df_sub$SP))]) +
  theme(axis.text.x = element_text(angle = 90, vjust = .5)) +
  ggtitle("Stand JBM 1 over the years") +
  # Add vertical separator lines BETWEEN certain years
  geom_vline(xintercept = c(3.5), linetype = "dashed", color = "red", size = 1)
```

------------------------------------------------------------------------

```{r}
# Subset
df_sub <- lgf2[lgf2$staplo == "JBM 2",]

# Create plot
#baskets may not correspond with other baskets in the same panel across red dotted line
ggplot(df_sub, aes(x = Year, y = mass, fill = SP)) + 
  geom_bar(stat = "identity", col = "black") + 
  theme_bw() +
  facet_wrap(~Basket, scales = "fixed", nrow = 2) +
  scale_fill_manual(values = litcol[1:length(unique(df_sub$SP))]) +
  theme(axis.text.x = element_text(angle = 90, vjust = .5)) +
  ggtitle("Stand JBM 2 over the years") +
  # Add vertical separator lines BETWEEN certain years
  geom_vline(xintercept = c(3.5), linetype = "dashed", color = "red", size = 1)
```

------------------------------------------------------------------------

```{r}
# Subset
df_sub <- lgf2[lgf2$staplo == "JBM 3",]

# Create plot
#baskets may not correspond with other baskets in the same panel across red dotted line
ggplot(df_sub, aes(x = Year, y = mass, fill = SP)) + 
  geom_bar(stat = "identity", col = "black") + 
  theme_bw() +
  facet_wrap(~Basket, scales = "fixed", nrow = 2) +
  scale_fill_manual(values = litcol[1:length(unique(df_sub$SP))]) +
  theme(axis.text.x = element_text(angle = 90, vjust = .5)) +
  ggtitle("Stand JBM 3 over the years") +
  # Add vertical separator lines BETWEEN certain years
  geom_vline(xintercept = c(3.5), linetype = "dashed", color = "red", size = 1)
```

------------------------------------------------------------------------

```{r}
# Subset
df_sub <- lgf2[lgf2$staplo == "JBM 4",]

# Create plot
#baskets may not correspond with other baskets in the same panel across red dotted line
ggplot(df_sub, aes(x = Year, y = mass, fill = SP)) + 
  geom_bar(stat = "identity", col = "black") + 
  theme_bw() +
  facet_wrap(~Basket, scales = "fixed", nrow = 2) +
  scale_fill_manual(values = litcol[1:length(unique(df_sub$SP))]) +
  theme(axis.text.x = element_text(angle = 90, vjust = .5)) +
  ggtitle("Stand JBM 4 over the years") +
  # Add vertical separator lines BETWEEN certain years
  geom_vline(xintercept = c(2.5), linetype = "dashed", color = "red", size = 1)
```

------------------------------------------------------------------------

```{r}

lgf2$Basket <- simp_baskets[as.character(lgf2$Basket)]

# Subset
df_sub <- lgf2[lgf2$staplo == "JBO 1",]

# Create plot
#baskets may not correspond with other baskets in the same panel across red dotted line
ggplot(df_sub, aes(x = Year, y = mass, fill = SP)) + 
  geom_bar(stat = "identity", col = "black") + 
  theme_bw() +
  facet_wrap(~Basket, scales = "fixed", nrow = 2) +
  scale_fill_manual(values = litcol[1:length(unique(df_sub$SP))]) +
  theme(axis.text.x = element_text(angle = 90, vjust = .5)) +
  ggtitle("Stand JBO 1 over the years") +
  # Add vertical separator lines BETWEEN certain years
  geom_vline(xintercept = c(3.5), linetype = "dashed", color = "red", size = 1)
```

------------------------------------------------------------------------

```{r}
# Subset
df_sub <- lgf2[lgf2$staplo == "JBO 2",]

# Create plot
#baskets may not correspond with other baskets in the same panel across red dotted line
ggplot(df_sub, aes(x = Year, y = mass, fill = SP)) + 
  geom_bar(stat = "identity", col = "black") + 
  theme_bw() +
  facet_wrap(~Basket, scales = "fixed", nrow = 2) +
  scale_fill_manual(values = litcol[1:length(unique(df_sub$SP))]) +
  theme(axis.text.x = element_text(angle = 90, vjust = .5)) +
  ggtitle("Stand JBO 2 over the years") +
  # Add vertical separator lines BETWEEN certain years
  geom_vline(xintercept = c(3.5), linetype = "dashed", color = "red", size = 1)
```

------------------------------------------------------------------------

```{r}
# Subset
df_sub <- lgf2[lgf2$staplo == "JBO 3",]

# Create plot
#baskets may not correspond with other baskets in the same panel across red dotted line
ggplot(df_sub, aes(x = Year, y = mass, fill = SP)) + 
  geom_bar(stat = "identity", col = "black") + 
  theme_bw() +
  facet_wrap(~Basket, scales = "fixed", nrow = 2) +
  scale_fill_manual(values = litcol[1:length(unique(df_sub$SP))]) +
  theme(axis.text.x = element_text(angle = 90, vjust = .5)) +
  ggtitle("Stand JBO 3 over the years") +
  # Add vertical separator lines BETWEEN certain years
  geom_vline(xintercept = c(3.5), linetype = "dashed", color = "red", size = 1)
```

------------------------------------------------------------------------

```{r}
# Subset
df_sub <- lgf2[lgf2$staplo == "JBO 4",]

# Create plot
#baskets may not correspond with other baskets in the same panel across red dotted line
ggplot(df_sub, aes(x = Year, y = mass, fill = SP)) + 
  geom_bar(stat = "identity", col = "black") + 
  theme_bw() +
  facet_wrap(~Basket, scales = "fixed", nrow = 2) +
  scale_fill_manual(values = litcol[1:length(unique(df_sub$SP))]) +
  theme(axis.text.x = element_text(angle = 90, vjust = .5)) +
  ggtitle("Stand JBO 4 over the years") +
  # Add vertical separator lines BETWEEN certain years
  geom_vline(xintercept = c(2.5), linetype = "dashed", color = "red", size = 1)
```

------------------------------------------------------------------------
